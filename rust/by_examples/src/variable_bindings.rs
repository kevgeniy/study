fn main() {
	let _unused_variable = 32;
	let noise_unused_variabel = 32;

	// Можно сначала объявлять переменные, а потом инициализировать их
	// Компилятор автоматически проверяет, чтобы мы не использовали неинициализированные переменные
}